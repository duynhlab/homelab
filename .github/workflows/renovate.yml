name: Renovate

on:
  schedule:
    - cron: "0 2 * * *"
  workflow_dispatch:

# Deny-all baseline; the job widens what it needs (cicd.md § permissions).
permissions: {}

jobs:
  renovate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          # Renovate authenticates with RENOVATE_TOKEN; the checkout must not
          # leave the ambient GITHUB_TOKEN in .git/config (cicd.md § checkout
          # in privileged jobs).
          persist-credentials: false
      - uses: renovatebot/github-action@dcfba84a42d1b5d5e49bf131b1bf53511851a123 # v46.3.1
        with:
          configurationFile: .renovaterc.json5
          token: ${{ secrets.RENOVATE_TOKEN }}
