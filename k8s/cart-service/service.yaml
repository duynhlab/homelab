apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: cart
  labels:
    app: cart-service
    component: api
spec:
  selector:
    app: cart-service
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
