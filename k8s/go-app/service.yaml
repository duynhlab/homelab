apiVersion: v1
kind: Service
metadata:
  name: demo-go-api
  namespace: monitoring-demo
  labels:
    app: demo-go-api
spec:
  type: NodePort
  selector:
    app: demo-go-api
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080
    name: http

