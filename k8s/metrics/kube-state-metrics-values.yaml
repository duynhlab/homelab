# Values for kube-state-metrics
# Installed via scripts/02-install-metrics.sh (Helm)

replicas: 1

# Ensure the chart installs into kube-system and uses standard service account/RBAC
rbac:
  create: true

serviceAccount:
  create: true
  name: kube-state-metrics

