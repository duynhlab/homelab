apiVersion: apps/v1
kind: Deployment
metadata:
  name: prometheus
  namespace: monitoring
  labels:
    app: prometheus
spec:
  replicas: 1
  selector:
    matchLabels:
      app: prometheus
  template:
    metadata:
      labels:
        app: prometheus
    spec:
      serviceAccountName: prometheus
      containers:
      - name: prometheus
        image: prom/prometheus:latest
        args:
        - '--config.file=/etc/prometheus/prometheus.yml'
        - '--storage.tsdb.path=/prometheus'
        - '--web.console.libraries=/usr/share/prometheus/console_libraries'
        - '--web.console.templates=/usr/share/prometheus/consoles'
        - '--web.enable-lifecycle'
        ports:
        - containerPort: 9090
          name: http
        volumeMounts:
        - name: prometheus-config
          mountPath: /etc/prometheus
        - name: prometheus-slo-rules
          mountPath: /etc/prometheus/slo-rules
          readOnly: true
          # Each ConfigMap file will be mounted as slo-rules.yaml
          # We need to rename them to service-specific names
        - name: prometheus-storage
          mountPath: /prometheus
        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "1Gi"
            cpu: "1000m"
      volumes:
      - name: prometheus-config
        configMap:
          name: prometheus-config
      - name: prometheus-slo-rules
        projected:
          sources:
          # Mount all service-specific SLO rule ConfigMaps
          # Each ConfigMap has a key like "auth-rules.yaml" which maps to the file name
          - configMap:
              name: prometheus-slo-rules-auth
              items:
              - key: auth-rules.yaml
                path: auth-rules.yaml
              optional: true
          - configMap:
              name: prometheus-slo-rules-user
              items:
              - key: user-rules.yaml
                path: user-rules.yaml
              optional: true
          - configMap:
              name: prometheus-slo-rules-product
              items:
              - key: product-rules.yaml
                path: product-rules.yaml
              optional: true
          - configMap:
              name: prometheus-slo-rules-cart
              items:
              - key: cart-rules.yaml
                path: cart-rules.yaml
              optional: true
          - configMap:
              name: prometheus-slo-rules-order
              items:
              - key: order-rules.yaml
                path: order-rules.yaml
              optional: true
          - configMap:
              name: prometheus-slo-rules-review
              items:
              - key: review-rules.yaml
                path: review-rules.yaml
              optional: true
          - configMap:
              name: prometheus-slo-rules-notification
              items:
              - key: notification-rules.yaml
                path: notification-rules.yaml
              optional: true
          - configMap:
              name: prometheus-slo-rules-shipping
              items:
              - key: shipping-rules.yaml
                path: shipping-rules.yaml
              optional: true
          - configMap:
              name: prometheus-slo-rules-shipping-v2
              items:
              - key: shipping-v2-rules.yaml
                path: shipping-v2-rules.yaml
              optional: true
      - name: prometheus-storage
        emptyDir: {}

