# PodMonitor for supporting-db PostgreSQL cluster
# Prometheus Operator will discover and scrape metrics from postgres_exporter sidecar
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  name: postgresql-supporting-db
  namespace: user
  labels:
    release: kube-prometheus-stack  # Required for Prometheus Operator discovery
spec:
  selector:
    matchLabels:
      application: spilo  # Zalando operator default label
      cluster-name: supporting-db  # Cluster-specific label
  podTargetLabels:
    - spilo-role
    - cluster-name
    - team
  podMetricsEndpoints:
    - port: exporter
      interval: 15s
      scrapeTimeout: 10s
