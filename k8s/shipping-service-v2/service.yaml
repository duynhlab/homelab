apiVersion: v1
kind: Service
metadata:
  name: shipping-service-v2
  namespace: shipping
  labels:
    app: shipping-service-v2
    component: api
spec:
  selector:
    app: shipping-service-v2
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
