apiVersion: v1
kind: Service
metadata:
  name: shipping-service
  namespace: shipping
  labels:
    app: shipping-service
    component: api
spec:
  selector:
    app: shipping-service
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
