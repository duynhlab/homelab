apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: user
  labels:
    app: user-service
    component: api
spec:
  selector:
    app: user-service
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP
