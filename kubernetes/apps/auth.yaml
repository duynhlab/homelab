---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: auth
  namespace: auth
spec:
  interval: 10m
  timeout: 10m
  chartRef:
    kind: OCIRepository
    name: mop-chart-oci
    namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  releaseName: auth
  values:
    name: auth

    replicaCount: 2

    image:
      repository: ghcr.io/duynhne/auth-service/auth
      tag: latest
      pullPolicy: Always

    service:
      type: ClusterIP
      port: 8080
      targetPort: 8080
      portName: http

    containerPort: 8080

    # Core configuration (chart-managed via env)
    # See charts/README.md for complete guide on env vs extraEnv
    env:
      - name: SERVICE_NAME
        value: "auth"
      - name: PORT
        value: "8080"
      - name: ENV
        value: "production"
      - name: OTEL_COLLECTOR_ENDPOINT
        value: "otel-collector-opentelemetry-collector.monitoring.svc.cluster.local:4318"
      - name: OTEL_SAMPLE_RATE
        value: "0.1"  # 10% sampling for production
      - name: PYROSCOPE_ENDPOINT
        value: "http://pyroscope.monitoring.svc.cluster.local:4040"
      - name: LOG_LEVEL
        value: "info"
      - name: LOG_FORMAT
        value: "json"
      - name: TRACING_ENABLED
        value: "true"
      - name: PROFILING_ENABLED
        value: "true"
      - name: METRICS_ENABLED
        value: "true"
      # Database configuration
      - name: DB_HOST
        value: "auth-db-pooler.auth.svc.cluster.local"  # PgBouncer pooler endpoint - auth-db is in auth namespace
      - name: DB_PORT
        value: "5432"
      - name: DB_NAME
        value: "auth"
      - name: DB_USER
        value: "auth"
      - name: DB_PASSWORD
        valueFrom:
          secretKeyRef:
            name: auth.auth-db.credentials.postgresql.acid.zalan.do  # Zalando operator auto-generated secret
            key: password
      - name: DB_SSLMODE
        value: "require"  # PgBouncer requires SSL connections
      - name: DB_POOL_MAX_CONNECTIONS
        value: "25"  # PgBouncer pool size
      - name: DB_POOL_MODE
        value: "transaction"  # PgBouncer transaction pooling
      # Graceful shutdown configuration
      - name: SHUTDOWN_TIMEOUT
        value: "10s"  # Default shutdown timeout (can be overridden)
      - name: READINESS_DRAIN_DELAY
        value: "5s"  # Delay after failing readiness, before HTTP shutdown

    # Kubernetes graceful shutdown configuration
    terminationGracePeriodSeconds: 30  # Shutdown timeout (10s) + buffer (20s)

    slo:
      enabled: true

    resources:
      requests:
        memory: "32Mi"
        cpu: "20m"
      limits:
        memory: "64Mi"
        cpu: "50m"

    livenessProbe:
      enabled: true
      httpGet:
        path: /health
        port: 8080
      initialDelaySeconds: 30
      periodSeconds: 10

    readinessProbe:
      enabled: true
      httpGet:
        path: /ready
        port: 8080
      initialDelaySeconds: 5
      periodSeconds: 5

    # Database migrations (Flyway init container)
    migrations:
      enabled: true
      image: ghcr.io/duynhne/auth-service/auth-init:latest
      imagePullPolicy: Always #IfNotPresent
      env:
        DB_HOST: "auth-db.auth.svc.cluster.local"  # Direct connection for migrations - auth-db is in auth namespace
        DB_PORT: "5432"
        DB_NAME: "auth"
        DB_USER: "auth"
        DB_SSLMODE: "require"
      envFrom:
        secretRef: "auth.auth-db.credentials.postgresql.acid.zalan.do"  # Zalando operator auto-generated secret
