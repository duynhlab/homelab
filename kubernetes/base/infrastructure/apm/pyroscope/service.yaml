apiVersion: v1
kind: Service
metadata:
  name: pyroscope
  namespace: monitoring
  labels:
    app: pyroscope
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 4040
    targetPort: 4040
    protocol: TCP
  selector:
    app: pyroscope

