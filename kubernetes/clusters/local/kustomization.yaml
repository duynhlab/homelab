---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - flux-system/
  - sources/
  - controllers.yaml     # Namespaces + operators (monitoring, databases, slo)
  - configs.yaml         # Instances (monitoring, apm, databases, slo)
  - apps.yaml            # Microservices