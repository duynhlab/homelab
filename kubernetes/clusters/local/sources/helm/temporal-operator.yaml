# RETIRED — NOT APPLIED (ADR-030 revision, RFC-0021 P3).
#
# The operator's HelmRepository is no longer listed in sources/kustomization.yaml
# (Flux prunes it from the cluster). Commented out in place — like the operator
# HelmRelease and the TemporalCluster/TemporalNamespace CRs — so every retired
# artifact of this re-platform is greppable in one shape.
#
# ---
# apiVersion: source.toolkit.fluxcd.io/v1
# kind: HelmRepository
# metadata:
#   name: temporal-operator
#   namespace: flux-system
#   labels:
#     app.kubernetes.io/component: helm-repository
# spec:
#   interval: 1h
#   url: https://alexandrevilain.github.io/temporal-operator
