---
# Production Cluster Configuration
# TODO: Configure FluxInstance for production cluster (TLS OCI, no insecure patch).
# See: ../local/sources/oci/README.md and ../local/flux-system/instance.yaml (production checklist comments).

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - flux-system/.gitkeep
  - sources/helm/.gitkeep

# Add Flux Kustomization CRDs here when ready:
# - infrastructure.yaml
# - monitoring.yaml
# - apm.yaml
# - databases.yaml
# - slo.yaml
# - apps.yaml
