apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - clusterissuers.yaml
  # Edge cert (platform-edge-tls) is owned by configs/envoy-gateway/certificate.yaml.
  - ca-source
  - bundles.yaml
