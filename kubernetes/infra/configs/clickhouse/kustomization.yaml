apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # Keeper first in reading order: the CHI references it by name and the
  # operator retries the CHI until the quorum answers (RFC-0028 / ADR-065).
  - clickhousekeeperinstallation.yaml
  - clickhouseinstallation.yaml
