apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Barman Cloud Plugin for CloudNativePG backup/restore integration.
# Applied after cert-manager so Certificate/Issuer resources can be created,
# and before database clusters that reference ObjectStore CRs.
resources:
  - barman-cloud-plugin.yaml
