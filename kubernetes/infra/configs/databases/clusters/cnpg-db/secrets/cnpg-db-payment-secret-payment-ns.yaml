# ExternalSecret for cnpg-db payment credentials — deployed to payment namespace
# Same Vault path as product-namespace copy; apps in payment ns need local secret
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cnpg-db-payment-secret
  namespace: payment
  labels:
    app.kubernetes.io/name: cnpg-db-payment-secret
    app.kubernetes.io/component: database
    app.kubernetes.io/managed-by: external-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: cnpg-db-payment-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/name: cnpg-db-payment-secret
          app.kubernetes.io/component: database
          app.kubernetes.io/managed-by: external-secrets
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/local/databases/cnpg-db/payment
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/local/databases/cnpg-db/payment
        property: password
