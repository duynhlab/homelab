# cart — per-service database triplet (RFC-0012, ADR-013).
#
# ExternalSecret for cnpg-db cart database credentials — product namespace.
# OpenBAO path: secret/data/local/databases/cnpg-db/cart
#
# Rendered as kubernetes.io/basic-auth (the pre-triplet copy was Opaque):
# this secret doubles as the passwordSecret for the DatabaseRole below, and
# CNPG requires basic-auth there. Secret `type` is immutable — migrating a
# live cluster requires deleting the old Opaque Secret so ESO recreates it
# (see docs/databases/runbooks/rotate-cnpg-service-password.md).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cnpg-db-cart-secret
  namespace: product
  labels:
    app.kubernetes.io/name: cnpg-db-cart-secret
    app.kubernetes.io/component: database
    app.kubernetes.io/managed-by: external-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: cnpg-db-cart-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: kubernetes.io/basic-auth
      metadata:
        labels:
          app.kubernetes.io/name: cnpg-db-cart-secret
          app.kubernetes.io/component: database
          app.kubernetes.io/managed-by: external-secrets
          cnpg.io/reload: "true"
      data:
        username: "{{ .username }}"
        password: "{{ .password }}"
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/local/databases/cnpg-db/cart
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/local/databases/cnpg-db/cart
        property: password
---
# Written out IN FULL — adoption resets omitted attributes (see payment.yaml).
apiVersion: postgresql.cnpg.io/v1
kind: DatabaseRole
metadata:
  name: cnpg-db-role-cart
  namespace: product
spec:
  cluster:
    name: cnpg-db
  name: cart
  comment: "cart-service application role (RFC-0012)"
  login: true
  superuser: false
  createdb: false
  createrole: false
  inherit: true
  replication: false
  bypassrls: false
  connectionLimit: -1
  inRoles: []
  passwordSecret:
    name: cnpg-db-cart-secret
  databaseRoleReclaimPolicy: retain
---
# Cart Database extensions (moved from extensions.yaml).
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: cart-database
  namespace: product
spec:
  name: cart
  owner: cart
  cluster:
    name: cnpg-db
  extensions:
    - name: pgaudit
    - name: pg_stat_statements
  databaseReclaimPolicy: retain
