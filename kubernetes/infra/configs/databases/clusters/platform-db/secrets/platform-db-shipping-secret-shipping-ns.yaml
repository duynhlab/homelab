# ExternalSecret for platform-db shipping credentials — deployed to shipping namespace
# Same Vault path as platform-namespace copy; apps in shipping ns need local secret
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: platform-db-shipping-secret
  namespace: shipping
  labels:
    app.kubernetes.io/name: platform-db-shipping-secret
    app.kubernetes.io/component: database
    app.kubernetes.io/managed-by: external-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: platform-db-shipping-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/name: platform-db-shipping-secret
          app.kubernetes.io/component: database
          app.kubernetes.io/managed-by: external-secrets
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/local/databases/shared-db/shipping
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/local/databases/shared-db/shipping
        property: password
