# ExternalSecret for platform-db user credentials — deployed to user namespace.
# Same Vault path as platform-namespace copy; apps in user ns need local secret.
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: platform-db-user-secret
  namespace: user
  labels:
    app.kubernetes.io/name: platform-db-user-secret
    app.kubernetes.io/component: database
    app.kubernetes.io/managed-by: external-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: platform-db-user-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/name: platform-db-user-secret
          app.kubernetes.io/component: database
          app.kubernetes.io/managed-by: external-secrets
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/local/databases/shared-db/user
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/local/databases/shared-db/user
        property: password
