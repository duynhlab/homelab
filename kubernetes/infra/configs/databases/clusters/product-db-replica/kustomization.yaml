apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# product-db-replica (DR cluster)
# Continuously recovers from product-db WAL archive via object store
resources:
  - objectstore.yaml
  - instance.yaml
