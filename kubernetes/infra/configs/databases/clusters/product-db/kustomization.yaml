apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# product-db cluster (CloudNativePG)
# Order: secrets → instance → extensions → poolers → monitoring
resources:
  - instance.yaml
  - poolers/
  - monitoring/podmonitor.yaml
  - secrets/product-db-secret.yaml
  - secrets/pg-backup-rustfs-credentials.yaml
  - backup/backup-daily.yaml
  - backup/backup-every-6h.yaml

  # - extensions.yaml