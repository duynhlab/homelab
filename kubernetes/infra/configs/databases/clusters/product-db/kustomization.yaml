apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# product-db cluster (CloudNativePG)
# Order: secrets → instance → poolers → monitoring
resources:
  - secrets/product-db-secret.yaml
  - instance.yaml
  - poolers/
  - monitoring/podmonitor-cloudnativepg-product-db.yaml
