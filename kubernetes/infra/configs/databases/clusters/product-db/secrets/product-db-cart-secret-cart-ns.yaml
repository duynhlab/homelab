# ExternalSecret for product-db cart credentials — deployed to cart namespace
# Same Vault path as product-namespace copy; apps in cart ns need local secret
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: product-db-cart-secret
  namespace: cart
  labels:
    app.kubernetes.io/name: product-db-cart-secret
    app.kubernetes.io/component: database
    app.kubernetes.io/managed-by: external-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: product-db-cart-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/name: product-db-cart-secret
          app.kubernetes.io/component: database
          app.kubernetes.io/managed-by: external-secrets
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/local/databases/product-db/cart
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/local/databases/product-db/cart
        property: password
