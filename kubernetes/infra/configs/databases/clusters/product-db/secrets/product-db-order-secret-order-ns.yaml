# ExternalSecret for product-db order credentials — deployed to order namespace
# Same Vault path as product-namespace copy; apps in order ns need local secret
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: product-db-order-secret
  namespace: order
  labels:
    app.kubernetes.io/name: product-db-order-secret
    app.kubernetes.io/component: database
    app.kubernetes.io/managed-by: external-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: openbao
    kind: ClusterSecretStore
  target:
    name: product-db-order-secret
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/name: product-db-order-secret
          app.kubernetes.io/component: database
          app.kubernetes.io/managed-by: external-secrets
  data:
    - secretKey: username
      remoteRef:
        key: secret/data/local/databases/product-db/order
        property: username
    - secretKey: password
      remoteRef:
        key: secret/data/local/databases/product-db/order
        property: password
