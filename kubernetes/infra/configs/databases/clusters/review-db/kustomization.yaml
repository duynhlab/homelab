apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# review-db cluster (Zalando Postgres Operator)
# Order: configmaps → instance
resources:
  - configmaps/monitoring-queries.yaml
  - configmaps/vector-sidecar.yaml
  - instance.yaml
