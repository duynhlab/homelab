# Cart Database Resource (CloudNativePG)
# Declaratively manages the cart database and extensions
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: cart-database
  namespace: cart
spec:
  name: cart
  owner: cart
  cluster:
    name: transaction-db
  extensions:
    - name: pgaudit
