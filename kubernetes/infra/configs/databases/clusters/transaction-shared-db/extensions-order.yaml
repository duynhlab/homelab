# Order Database Resource (CloudNativePG)
# Declaratively manages the order database and extensions
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: order-database
  namespace: cart
spec:
  name: order
  owner: cart
  cluster:
    name: transaction-shared-db
  extensions:
    - name: pgaudit
