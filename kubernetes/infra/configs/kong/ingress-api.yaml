# API routes on the public gateway (gateway.duynhne.me).
#
# URL shape: /{service}/v1/{audience}/{resource...}  — Variant A
# (see docs/api/api-naming-convention.md). Services mount these paths
# directly; Kong does NOT rewrite — pure pass-through.
#
# Only `public` and `private` audiences are exposed on the gateway.
# `internal` routes (service-to-service) are deliberately excluded
# and reachable only via in-cluster Service DNS.
#
# Services keep validating JWTs themselves in their middleware
# (defense-in-depth; Kong does not enforce auth).
#
# TLS is terminated by Kong using the default proxy certificate
# (kong-proxy-tls secret, mounted via the HelmRelease secretVolumes).

---
# Auth Service — public (login, register) + private (me)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-auth
  namespace: auth
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/plugins: rate-limiting-api,request-size-limiting-api
spec:
  ingressClassName: kong
  rules:
    - host: gateway.duynhne.me
      http:
        paths:
          - path: /auth/v1/public/
            pathType: Prefix
            backend:
              service:
                name: auth
                port:
                  number: 8080
          - path: /auth/v1/private/
            pathType: Prefix
            backend:
              service:
                name: auth
                port:
                  number: 8080
---
# User Service — public GET /users/:id + private /users/profile
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-user
  namespace: user
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/plugins: rate-limiting-api,request-size-limiting-api
spec:
  ingressClassName: kong
  rules:
    - host: gateway.duynhne.me
      http:
        paths:
          - path: /user/v1/public/
            pathType: Prefix
            backend:
              service:
                name: user
                port:
                  number: 8080
          - path: /user/v1/private/
            pathType: Prefix
            backend:
              service:
                name: user
                port:
                  number: 8080
---
# Product Service — public catalog (list, detail, aggregated details)
# (POST /product/v1/internal/products is NOT exposed here)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-product
  namespace: product
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/plugins: rate-limiting-api,request-size-limiting-api
spec:
  ingressClassName: kong
  rules:
    - host: gateway.duynhne.me
      http:
        paths:
          - path: /product/v1/public/
            pathType: Prefix
            backend:
              service:
                name: product
                port:
                  number: 8080
---
# Cart Service — private only (all routes require JWT)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-cart
  namespace: cart
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/plugins: rate-limiting-api,request-size-limiting-api
spec:
  ingressClassName: kong
  rules:
    - host: gateway.duynhne.me
      http:
        paths:
          - path: /cart/v1/private/
            pathType: Prefix
            backend:
              service:
                name: cart
                port:
                  number: 8080
---
# Order Service — private only (all routes require JWT)
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-order
  namespace: order
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/plugins: rate-limiting-api,request-size-limiting-api
spec:
  ingressClassName: kong
  rules:
    - host: gateway.duynhne.me
      http:
        paths:
          - path: /order/v1/private/
            pathType: Prefix
            backend:
              service:
                name: order
                port:
                  number: 8080
---
# Review Service — public list + private submit
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-review
  namespace: review
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/plugins: rate-limiting-api,request-size-limiting-api
spec:
  ingressClassName: kong
  rules:
    - host: gateway.duynhne.me
      http:
        paths:
          - path: /review/v1/public/
            pathType: Prefix
            backend:
              service:
                name: review
                port:
                  number: 8080
          - path: /review/v1/private/
            pathType: Prefix
            backend:
              service:
                name: review
                port:
                  number: 8080
---
# Notification Service — private only (user notifications).
# /notification/v1/internal/notify/{email,sms} is NOT exposed here.
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-notification
  namespace: notification
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/plugins: rate-limiting-api,request-size-limiting-api
spec:
  ingressClassName: kong
  rules:
    - host: gateway.duynhne.me
      http:
        paths:
          - path: /notification/v1/private/
            pathType: Prefix
            backend:
              service:
                name: notification
                port:
                  number: 8080
---
# Shipping Service — public track + estimate only.
# /shipping/v1/internal/orders/:orderId is NOT exposed here
# (called only by order-service via in-cluster DNS).
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: api-shipping
  namespace: shipping
  annotations:
    konghq.com/strip-path: "false"
    konghq.com/plugins: rate-limiting-api,request-size-limiting-api
spec:
  ingressClassName: kong
  rules:
    - host: gateway.duynhne.me
      http:
        paths:
          - path: /shipping/v1/public/
            pathType: Prefix
            backend:
              service:
                name: shipping
                port:
                  number: 8080
