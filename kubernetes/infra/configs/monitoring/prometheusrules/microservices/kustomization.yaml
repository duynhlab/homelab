apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - alerts.yaml
  - alerts-otel.yaml # RFC-0014 P2 new-name copies (retire alerts.yaml at P3)
  - recording-rules.yaml
  - recording-rules-otel.yaml # RFC-0014 P2 (retire recording-rules.yaml at P3)
