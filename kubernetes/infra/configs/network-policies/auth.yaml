# kindnet does not enforce NetworkPolicy; these are authored ready and become
# effective once an enforcing CNI (Cilium/Calico) is installed.
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deny-all-ingress
  namespace: auth
spec:
  podSelector: {}
  policyTypes:
    - Ingress
---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: allow-internal-callers
  namespace: auth
spec:
  podSelector: {}
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: kong
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: auth
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: user
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: product
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: cart
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: order
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: review
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: notification
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: shipping
      ports:
        - protocol: TCP
          port: 8080
