apiVersion: grafana.integreatly.org/v1beta1
kind: Grafana
metadata:
  name: grafana
  namespace: monitoring
  labels:
    dashboards: grafana
spec:
  config:
    auth:
      disable_login_form: "true"
    auth.anonymous:
      enabled: "true"
      org_role: Admin
    log:
      level: info
      mode: console
    plugins:
      allow_loading_unsigned_plugins: victoriametrics-metrics-datasource,victoriametrics-logs-datasource
    server:
      root_url: http://localhost:3000
    users:
      default_theme: dark
  deployment:
    spec:
      replicas: 1
      template:
        spec:
          containers:
          - image: grafana/grafana:13.1.0
            imagePullPolicy: IfNotPresent
            name: grafana
            env:
            - name: GF_INSTALL_PLUGINS
              value: "https://github.com/VictoriaMetrics/victoriametrics-datasource/releases/download/v0.25.2/victoriametrics-metrics-datasource-v0.25.2.zip;victoriametrics-metrics-datasource,https://github.com/VictoriaMetrics/victorialogs-datasource/releases/download/v0.29.0/victoriametrics-logs-datasource-v0.29.0.zip;victoriametrics-logs-datasource,grafana-clickhouse-datasource"
            resources:
              limits:
                # 300m: 100m throttled continuously (KubePodCPUThrottlingHigh).
                cpu: 300m
                memory: 512Mi
              requests:
                cpu: 100m
                memory: 128Mi
  service:
    metadata:
      labels:
        app: grafana
    spec:
      type: ClusterIP
      ports:
      - name: http
        port: 3000
        targetPort: 3000
        protocol: TCP
