apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - kubernetes/
  - microservices/
  - postgres/
  - kong/
  - victoriametrics/
  - observability/
  - valkey/
  - gitops/
  - temporal/ # workflow-engine health (moved here from configs/temporal — every other PrometheusRule already lived under this tree)
  - watchdog.yaml
