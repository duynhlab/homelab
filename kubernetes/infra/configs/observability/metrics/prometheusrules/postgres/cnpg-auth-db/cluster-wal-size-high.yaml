# CNPG-only: WAL directory size (legacy split from postgres-alerts).
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: auth-db-cluster-wal-size-high
  namespace: auth
  labels:
    app.kubernetes.io/part-of: cnpg-chart-rules
    cnpg.io/cluster: auth-db
spec:
  groups:
    - name: cloudnative-pg/auth-db-wal
      interval: 5m
      rules:
        - alert: PostgresWALSizeHigh
          expr: cnpg_collector_pg_wal{value="size", namespace="auth"} > 2e+9
          for: 15m
          labels:
            severity: warning
          annotations:
            summary: "CNPG WAL size high ({{ $labels.namespace }}/{{ $labels.cluster }})"
            description: "WAL directory size is {{ $value | humanize1024 }}B (threshold: 2GB)."
