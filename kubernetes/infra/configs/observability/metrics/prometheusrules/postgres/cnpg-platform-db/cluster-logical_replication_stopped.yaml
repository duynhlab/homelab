# Generated from cnpg/cluster chart v0.6.0 (helm template) with fullnameOverride=platform-db, namespace=platform.
# Do not edit expr by hand; re-render from chart and split, or bump chart version in README.
---
apiVersion: monitoring.coreos.com/v1
kind: PrometheusRule
metadata:
  name: platform-db-cluster-logical-replication-stopped
  namespace: platform
  labels:
    app.kubernetes.io/part-of: cnpg-chart-rules
    cnpg.io/cluster: platform-db
    platform.duynhlab.dev/source: cloudnative-pg-charts-cluster
    platform.duynhlab.dev/chart-version: 0.6.0
spec:
  groups:
  - name: cloudnative-pg/platform-db
    rules:
    - alert: CNPGClusterLogicalReplicationStopped
      annotations:
        summary: CNPG Cluster logical replication subscription stopped
        description: "CloudNativePG Cluster's \"platform/platform-db\" \"{{ .subname }}\" subscription is stopped.\n\nStatus: \n\nThe subscription is not actively replicating data. This could be intentional (disabled) or due to an issue preventing the subscription from working."
        runbook_url: https://github.com/cloudnative-pg/charts/blob/main/charts/cluster/docs/runbooks/CNPGClusterLogicalReplicationStopped.md
      expr: "(\n  # Subscription is explicitly disabled\n  label_replace(max by (namespace, job, subname) (cnpg_pg_stat_subscription_enabled), \"cluster\", \"$1\", \"job\", \".+/(.+)\") == 0\n) or (\n  # Subscription is enabled but stuck (no worker process with significant lag)\n  label_replace(max by (namespace, job, subname) (cnpg_pg_stat_subscription_enabled), \"cluster\", \"$1\", \"job\", \".+/(.+)\") == 1\n  and label_replace(max by (namespace, job, subname) (cnpg_pg_stat_subscription_pid), \"cluster\", \"$1\", \"job\", \".+/(.+)\") == \"\"\n  and label_replace(max by (namespace, job, subname) (cnpg_pg_stat_subscription_buffered_lag_bytes), \"cluster\", \"$1\", \"job\", \".+/(.+)\") / 1024^3 > 0.1\n)"
      for: 5m
      labels:
        severity: warning
        namespace: platform
        cnpg_cluster: platform-db
