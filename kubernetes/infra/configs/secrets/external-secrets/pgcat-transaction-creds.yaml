# ExternalSecret for PgCat pooler credentials (Cart namespace)
# Creates shadow secret: pgcat-transaction-credentials-vault
#
# NOTE: PgCat uses raw ConfigMap for configuration.
# This ExternalSecret is created for:
# 1. InitContainer-based config rendering (if implemented)
# 2. Reference for password values in Vault
#
# Current workaround: Inline passwords in ConfigMap (dev-only, documented)
# See: kubernetes/infra/configs/databases/clusters/transaction-db/poolers/configmap.yaml
#
# Vault path: secret/data/poolers/pgcat-transaction
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: pgcat-transaction-credentials-vault
  namespace: cart
  labels:
    app.kubernetes.io/name: pgcat-transaction-credentials
    app.kubernetes.io/component: pooler
    app.kubernetes.io/managed-by: external-secrets
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-dev
    kind: ClusterSecretStore
  target:
    name: pgcat-transaction-credentials-vault
    creationPolicy: Owner
    deletionPolicy: Retain
    template:
      type: Opaque
      metadata:
        labels:
          app.kubernetes.io/name: pgcat-transaction-credentials
          app.kubernetes.io/component: pooler
          app.kubernetes.io/managed-by: external-secrets
  data:
    - secretKey: admin_username
      remoteRef:
        key: secret/data/poolers/pgcat-transaction
        property: admin_username
    - secretKey: admin_password
      remoteRef:
        key: secret/data/poolers/pgcat-transaction
        property: admin_password
    - secretKey: db_username
      remoteRef:
        key: secret/data/poolers/pgcat-transaction
        property: db_username
    - secretKey: db_password
      remoteRef:
        key: secret/data/poolers/pgcat-transaction
        property: db_password
