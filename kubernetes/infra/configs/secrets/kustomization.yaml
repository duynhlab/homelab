apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Secrets management configuration:
# - Vault bootstrap (Kubernetes auth setup)
# - ClusterSecretStore for ESO
# - ExternalSecrets for DB credentials (Phase 2)
resources:
  - vault-bootstrap/
  - cluster-secret-store.yaml
  - external-secrets/
