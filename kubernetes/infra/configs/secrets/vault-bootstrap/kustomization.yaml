apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Vault bootstrap resources for dev mode
# These configure Kubernetes auth and ESO role on each Vault restart
resources:
  - serviceaccount.yaml
  - clusterrolebinding.yaml
  - configmap.yaml
  - job.yaml
