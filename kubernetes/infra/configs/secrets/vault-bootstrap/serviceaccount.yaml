# ServiceAccount for Vault bootstrap Job
# Needs permissions to:
# - Read Kubernetes API server CA cert
# - Create TokenReview for Vault Kubernetes auth config
apiVersion: v1
kind: ServiceAccount
metadata:
  name: vault-bootstrap
  namespace: vault
  labels:
    app.kubernetes.io/name: vault-bootstrap
    app.kubernetes.io/component: bootstrap
