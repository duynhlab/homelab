apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - helmrelease.yaml # official temporalio chart (replaced the operator CRs — ADR-030 revision)
  # - cluster.yaml   # RETIRED: TemporalCluster CR (operator), commented out in-file
  # - namespace.yaml # RETIRED: TemporalNamespace CR — `mop` now via chart namespace job
  - ingress.yaml
  - prometheusrule.yaml
