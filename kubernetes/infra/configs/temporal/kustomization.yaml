apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Config half of Temporal — the chart is controllers/temporal.
# The `.bak` files are the retired operator's CRs, kept readable and unapplied.
resources:
  - prometheusrule.yaml
