apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# APM components are infrastructure (not CRDs), deployed in controllers layer
resources:
  - loki/
  - tempo/
  - pyroscope/
  - vector/vector.yaml
  - jaeger/jaeger.yaml
  - otel-collector/otel-collector.yaml
