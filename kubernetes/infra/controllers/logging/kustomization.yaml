apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Logging stack: VictoriaLogs (storage) + Vector (collector)
# VictoriaLogs is operator-managed via VLSingle CRD in configs/monitoring/victoriametrics/
resources:
  - vector/vector.yaml
