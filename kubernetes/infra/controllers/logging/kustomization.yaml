apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Logging stack: VictoriaLogs (storage) + Vector (collector)
# VictoriaLogs is operator-managed via VLSingle CRD in configs/observability/metrics/victoriametrics/
resources:
  - vector/vector.yaml
