apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Logging stack: Loki (storage) + Vector (collector) + VictoriaLogs (secondary storage)
resources:
  - loki/
  - vector/vector.yaml
  - victorialogs/helmrelease.yaml
