apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Object storage: RustFS (S3-compatible)
resources:
  - rustfs/helmrelease.yaml
  # One-time Job to create pg-backups bucket (idempotent, runs after RustFS is up)
  - rustfs/job-create-bucket.yaml
