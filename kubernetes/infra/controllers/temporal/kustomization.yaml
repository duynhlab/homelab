apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# RETIRED (ADR-030 revision): the temporal-operator HelmRelease in this
# directory is commented out — the official temporalio chart in
# configs/temporal/ replaced it. This directory is no longer referenced by
# controllers/kustomization.yaml; the empty resource list keeps a stray
# `kustomize build` of this path harmless.
resources: []
