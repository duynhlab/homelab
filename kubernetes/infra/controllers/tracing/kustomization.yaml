apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Tracing stack: Tempo (storage) + Jaeger (UI) + OTel Collector (pipeline)
resources:
  - tempo/
  - jaeger/jaeger.yaml
  - otel-collector/otel-collector.yaml
