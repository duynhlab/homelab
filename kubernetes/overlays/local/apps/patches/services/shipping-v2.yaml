#############################################################################
# Shipping Service (v2) - Local overrides
#############################################################################
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: shipping-v2
  namespace: shipping
spec:
  values:
    replicaCount: 1
    
    resources:
      requests:
        memory: "32Mi"
        cpu: "25m"
      limits:
        memory: "64Mi"
        cpu: "50m"
    
    env:
      - name: SERVICE_NAME
        value: "shipping-v2"
      - name: PORT
        value: "8080"
      - name: ENV
        value: "local"
      - name: OTEL_COLLECTOR_ENDPOINT
        value: "otel-collector-opentelemetry-collector.monitoring.svc.cluster.local:4318"
      - name: OTEL_SAMPLE_RATE
        value: "1.0"
      - name: PYROSCOPE_ENDPOINT
        value: "http://pyroscope.monitoring.svc.cluster.local:4040"
      - name: LOG_LEVEL
        value: "debug"
      - name: LOG_FORMAT
        value: "json"
      - name: TRACING_ENABLED
        value: "true"
      - name: PROFILING_ENABLED
        value: "true"
      - name: METRICS_ENABLED
        value: "true"
      - name: DB_HOST
        value: "supporting-db.shipping.svc.cluster.local"
      - name: DB_PORT
        value: "5432"
      - name: DB_NAME
        value: "shipping"
      - name: DB_USER
        value: "shipping"
      - name: DB_PASSWORD
        valueFrom:
          secretKeyRef:
            name: shipping.supporting-db.credentials.postgresql.acid.zalan.do
            key: password
      - name: DB_SSLMODE
        value: "require"
      - name: DB_POOL_MAX_CONNECTIONS
        value: "10"
      - name: DB_POOL_MODE
        value: "session"
      - name: SHUTDOWN_TIMEOUT
        value: "10s"

