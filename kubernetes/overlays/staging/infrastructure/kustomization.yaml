---
# Staging Infrastructure Overlay
# TODO: Add staging-specific infrastructure patches
# Example: different storage sizes, retention periods, resource limits

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/infrastructure

# Staging-specific labels
labels:
  - pairs:
      environment: staging
